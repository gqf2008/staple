[package]
name = "staple-domain"
description = "Staple domain models and business rules (pure Rust, no I/O)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde_json = "1"
